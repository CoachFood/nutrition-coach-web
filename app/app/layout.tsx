export const metadata = {
  title: "CoachFood",
  description: "Coach nutrition intelligent"
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="fr">
      <body>{children}</body>
    </html>
  );
}
